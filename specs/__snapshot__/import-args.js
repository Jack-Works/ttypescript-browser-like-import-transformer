function __dynamicImportTransformFailedHelper2(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1]);
}
function __dynamicImportTransformFailedHelper3(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2]);
}
function __dynamicImportTransformFailedHelper4(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3]);
}
function __dynamicImportTransformFailedHelper5(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4]);
}
function __dynamicImportTransformFailedHelper6(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4], args[5]);
}
Promise.resolve(globalThis.0);
__dynamicImportTransformFailedHelper2("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1);
__dynamicImportTransformFailedHelper3("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2);
__dynamicImportTransformFailedHelper4("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3);
__dynamicImportTransformFailedHelper5("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3, 4);
__dynamicImportTransformFailedHelper6("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3, 4, 5);
