Promise.resolve(globalThis.0);
__dynImport2Ary("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1);
__dynImport3Ary("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2);
__dynImport4Ary("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3);
__dynImport5Ary("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3, 4);
__dynImport6Ary("Transform rule found, but this dynamic import has more than 1 argument, don't know how to transform that.", '0', 1, 2, 3, 4, 5);
function __dynImport2Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1]);
}
function __dynImport3Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2]);
}
function __dynImport4Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3]);
}
function __dynImport5Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4]);
}
function __dynImport6Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4], args[5]);
}
