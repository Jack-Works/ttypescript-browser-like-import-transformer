// CompilerOptions: {"module":"ESNext"}
// PluginConfig: {}
Promise.resolve(globalThis["0"]);
__dynamicImport2Ary("@magic-works/ttypescript-browser-like-import-transformer: Transform rule for this dependencies found, but this dynamic import has more than 1 argument, transformer don't know how to transform that and keep it untouched.", '0', 1);
__dynamicImport3Ary("@magic-works/ttypescript-browser-like-import-transformer: Transform rule for this dependencies found, but this dynamic import has more than 1 argument, transformer don't know how to transform that and keep it untouched.", '0', 1, 2);
__dynamicImport4Ary("@magic-works/ttypescript-browser-like-import-transformer: Transform rule for this dependencies found, but this dynamic import has more than 1 argument, transformer don't know how to transform that and keep it untouched.", '0', 1, 2, 3);
__dynamicImport5Ary("@magic-works/ttypescript-browser-like-import-transformer: Transform rule for this dependencies found, but this dynamic import has more than 1 argument, transformer don't know how to transform that and keep it untouched.", '0', 1, 2, 3, 4);
__dynamicImport6Ary("@magic-works/ttypescript-browser-like-import-transformer: Transform rule for this dependencies found, but this dynamic import has more than 1 argument, transformer don't know how to transform that and keep it untouched.", '0', 1, 2, 3, 4, 5);
function __dynamicImport2Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1]);
}
function __dynamicImport3Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2]);
}
function __dynamicImport4Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3]);
}
function __dynamicImport5Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4]);
}
function __dynamicImport6Ary(reason, ...args) {
    console.warn(reason, ...args);
    return import(args[0], args[1], args[2], args[3], args[4], args[5]);
}
